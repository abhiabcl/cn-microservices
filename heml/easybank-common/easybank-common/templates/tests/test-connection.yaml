apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easybank-common.fullname" . }}-test-connection"
  labels:
    {{- include "easybank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easybank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
